import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prueba-react-app-moviles',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
